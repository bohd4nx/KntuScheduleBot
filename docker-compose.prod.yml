services:
  bot:
    container_name: kntu-schedule-bot
    image: kntu-schedule-bot
    build: .
    restart: unless-stopped
    env_file:
    - .env
    networks:
    - bots_network

networks:
  bots_network:
    external: true
